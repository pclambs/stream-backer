import { useQuery } from '@apollo/client';
import { QUERY_PROFILES } from '../utils/queries'

const Home = () => {

  return (
    <main>
      <h2> Hello World! </h2>
      
    </main>
  )
}

export default Home;
