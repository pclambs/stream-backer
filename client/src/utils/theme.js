import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: '#bd279f',
  
    },
    secondary: {
      main: '#F3F6F9',
    },
    info: {
      main: '#808080',
    },
  },
})

export default theme